name: Deploy via SSH

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader

      - name: Prepare .env for production
        run: |
          if [ -f .env.prod ]; then
            cp .env.prod .env
          fi

      - name: Deploy via SSH (rsync)
        uses: easingthemes/ssh-deploy@v5.1.0
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          ARGS: "-rlgoDzvc -i --delete"
          REMOTE_HOST: 89.117.169.28
          REMOTE_USER: u602093072
          REMOTE_PORT: 65002
          TARGET: /home/u602093072/domains/mindmagik.in/public_html/diawinhms
          EXCLUDE: "/.git/, /.github/, /node_modules/, /tests/, .env.local, .env.prod, /assets/libs/**/src/, /assets/libs/**/test/, /assets/libs/**/docs/"
